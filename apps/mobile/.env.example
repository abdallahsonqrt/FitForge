# Base URL of the FitForge API.
# Required for release builds. In development the app falls back to the Metro
# host on port 3000, so a phone on the same LAN reaches your machine.
EXPO_PUBLIC_API_URL=http://localhost:3000
